/** useState定义 */

export const useState = <T>(initialValue: T) => {
    let value = initialValue;
    const valueUUid = Math.random().toString(36).slice(2);
    const v_state = value;
    Proxy
    const getValue = ()=>{

    }
}